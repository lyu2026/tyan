name: 签名打包

on:
  push:
    tags:
      - 'v*'
      - 'release-*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 拉取项目代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 配置 JV17 库
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: 配置 N20 库
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 安装安卓SDK
        uses: android-actions/setup-android@v3

      - name: 安装打包组件
        run: |
          sdkmanager \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0"

      - name: 安装科尔多瓦
        run: npm install -g cordova@latest

      - name: 安装项目依赖
        run: npm install

      - name: 添加安卓平台
        run: cordova platform add android

      - name: 修改 AndroidManifest
        run: |
          MANIFEST="platforms/android/app/src/main/AndroidManifest.xml"
          if grep -q 'requestLegacyExternalStorage' "$MANIFEST"; then
            echo "✓ requestLegacyExternalStorage 已存在，跳过"
          else
            sed -i 's|<application|<application android:requestLegacyExternalStorage="true"|' "$MANIFEST"
            echo "✓ 已添加 requestLegacyExternalStorage"
          fi
          if grep -q 'networkSecurityConfig' "$MANIFEST"; then
            echo "✓ networkSecurityConfig 已存在，跳过"
          else
            sed -i 's|<application|<application android:networkSecurityConfig="@xml/network_security_config"|' "$MANIFEST"
            echo "✓ 已注册 networkSecurityConfig"
          fi

      - name: 创建网络安全配置
        run: |
          mkdir -p platforms/android/app/src/main/res/xml
          printf '%s\n' \
            '<?xml version="1.0" encoding="utf-8"?>' \
            '<network-security-config>' \
            '  <base-config cleartextTrafficPermitted="true">' \
            '    <trust-anchors>' \
            '      <certificates src="system" />' \
            '      <certificates src="user" />' \
            '    </trust-anchors>' \
            '  </base-config>' \
            '</network-security-config>' \
            > platforms/android/app/src/main/res/xml/network_security_config.xml
          echo "✓ 网络安全配置已写入"

      - name: 解密签名信息
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > "$GITHUB_WORKSPACE/tyan.keystore"

      - name: 区分架构打包
        run: |
          KEYSTORE_PATH="$GITHUB_WORKSPACE/tyan.keystore"
          ABIS=("arm64-v8a" "armeabi-v7a" "x86_64")
          mkdir -p output

          for ABI in "${ABIS[@]}"; do
            echo ">>> 正在打包架构：$ABI"

            cordova clean android

            mkdir -p platforms/android/app/src/main/res/drawable-nodpi
            mkdir -p platforms/android/app/src/main/res/values
            mkdir -p platforms/android/app/src/main/res/values-night

            cordova build android --release -- \
              --packageType=apk \
              --gradleArg="-Pandroid.defaultConfig.abiFilters=$ABI" \
              --keystore="$KEYSTORE_PATH" \
              --storePassword="Tyan2026" \
              --alias="tyan" \
              --password="Tyan2026"

            cp platforms/android/app/build/outputs/apk/release/app-release.apk \
               output/${ABI}.apk

            echo ">>> $ABI 打包完成"
          done

      - name: 获取版本编号
        id: version
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          VERSION=$(echo "$TAG_NAME" | sed 's/^v//' | sed 's/^release-//')
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 获取构建时间
        id: time
        run: |
          export TZ='Asia/Shanghai'
          echo "datetime=$(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_OUTPUT

      - name: 生成发布信息
        id: apk_info
        run: |
          cd output
          {
            echo "### 📦 APK 文件清单"
            echo ""
            echo "| 文件名 | 大小 | SHA256 |"
            echo "|--------|------|--------|"
            for apk in *.apk; do
              SIZE=$(ls -lh "$apk" | awk '{print $5}')
              SHA256=$(sha256sum "$apk" | cut -d ' ' -f 1)
              echo "| $apk | $SIZE | \`$SHA256\` |"
            done
          } > ../release_body.md
          cd ..

          CURRENT_TAG="${{ steps.version.outputs.tag_name }}"
          PREV_TAG=$(git tag --sort=-creatordate | grep -v "^${CURRENT_TAG}$" | head -1)

          if [ -n "$PREV_TAG" ]; then
            {
              echo "### 📝 更新内容"
              echo ""
              git log "${PREV_TAG}..${CURRENT_TAG}" \
                --pretty=format:"- %s (%h, @%an)" \
                --no-merges
              echo ""
            } > release_changelog.md
          else
            printf "### 📝 更新内容\n\n首次发布 🎉\n" > release_changelog.md
          fi

          CHANGELOG=$(cat release_changelog.md)
          MANIFEST=$(cat release_body.md)
          VERSION="${{ steps.version.outputs.version }}"
          DATETIME="${{ steps.time.outputs.datetime }}"

          {
            echo "## 🫧 廷琰 V${VERSION}"
            echo ""
            echo "### 📦 版本信息"
            echo "| 项目 | 信息 |"
            echo "|------|------|"
            echo "| 版本号 | V${VERSION} |"
            echo "| 构建时间 | ${DATETIME} |"
            echo "| 构建类型 | Release（签名） |"
            echo ""
            echo "${CHANGELOG}"
            echo ""
            echo "${MANIFEST}"
            echo ""
            echo "### 📥 安装说明"
            echo "1. 根据手机 CPU 架构选择对应的 APK"
            echo "   - 大多数现代手机选择 \`arm64-v8a\`"
            echo "   - 老旧手机选择 \`armeabi-v7a\`"
            echo "   - 模拟器选择 \`x86_64\`"
            echo "2. 下载后在手机上允许\"安装未知来源应用\""
            echo "3. 点击 APK 进行安装"
            echo ""
            echo "### 🔍 查看 CPU 架构"
            echo "如不确定手机架构，可安装 [Device Info HW](https://play.google.com/store/apps/details?id=ru.andr7e.deviceinfohw) 查看"
          } > release_body_full.md

      - name: 发布并上传包
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: 廷琰 v${{ steps.version.outputs.version }}
          body_path: release_body_full.md
          files: output/*.apk
          generate_release_notes: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建完成通知
        if: success()
        run: |
          echo "✅ 所有 APK 包构建成功"
          ls -lh output/

      - name: 构建失败通知
        if: failure()
        run: |
          echo "❌ 构建失败，请检查日志"
          exit 1
